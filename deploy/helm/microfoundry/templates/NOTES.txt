MicroFoundry has been deployed!

{{- if .Values.ingress.enabled }}
Access the admin UI at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
Get the application URL by running:
  kubectl port-forward -n {{ .Values.namespace }} svc/{{ include "microfoundry.fullname" . }} 8080:{{ .Values.service.port }}
  Then visit: http://localhost:8080
{{- end }}

{{- if not .Values.auth.enabled }}

NOTE: Authentication is disabled. Enable it by setting auth.enabled=true
and configuring your OIDC provider.
{{- end }}
